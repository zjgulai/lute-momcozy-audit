# Lute Momcozy Audit

Public, sanitized technical audit. The site deliberately excludes real business
KPIs, monetary ROI, private infrastructure, internal paths, and raw evidence.

```bash
npm ci
npm test
```

All pages are generated from one allowlisted public dataset. The old public
history is retained only in the private archive repository.

